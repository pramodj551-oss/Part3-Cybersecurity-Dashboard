# Production dependencies are generated/locked from requirements.in.
# Do not add developer-only dependencies here.
# The authoritative production lock is generated by pip-compile with SHA256 hashes.

# Temporary guard: production CI must reject this non-lock file until the generated
# 50-package hashed lock is committed by the lock workflow.
